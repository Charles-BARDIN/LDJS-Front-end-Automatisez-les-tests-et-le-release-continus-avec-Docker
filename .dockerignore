# Dependencies (reinstalled inside the image)
node_modules

# Build output (regenerated by the build stage)
dist
build

# Tests and coverage (not needed to build the bundle)
tests
coverage

# Version control and metadata
.git
.gitignore

# Docker files themselves
Dockerfile
.dockerignore
docker-compose.yml

# Editor / OS noise
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Docs and logs
*.md
*.log
npm-debug.log*

# Misc project templates not used by the image
values.tpl.yaml
